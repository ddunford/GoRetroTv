{"id":"int-81bd3daa304f9aad65854419adc1d661","kind":"field_change","created_at":"2026-09-16T16:05:51.828341025Z","actor":"Dan Dunford","issue_id":"gort-6ar.19","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"N/A and recorded, not skipped. Phase 1 ships no user-facing surface — a Go binary, a machine-readable health endpoint, a CLI comparison tool, CI config. No screen to walk, nothing for axe to scan, no layout at 320px. These three gates attach to phase 5 (gort-4sx), which builds the browser client."}}
{"id":"int-4cd002caf632b02ecdb530c56d9b6968","kind":"field_change","created_at":"2026-09-16T16:18:12.791905107Z","actor":"Dan Dunford","issue_id":"gort-6ar.1","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Scaffold shipped and VERIFIED BY RUNNING IT (commit 0fa8ea3). Module github.com/ddunford/goretrotv at root, binaries under cmd/, shared internal/. Dockerfile (distroless static, CGO_ENABLED=0), docker-compose.yml (loopback-only publish, firmware mounted read-only never baked in), Makefile, ctl.sh, .golangci.yml v2, two ADRs.\n\nObserved, not inferred: ./ctl.sh up -> healthy with /health naming the real build; listener bound 127.0.0.1 only and a real connection to http://<lan-ip>:8099/health REFUSED; SIGTERM -> graceful shutdown, clean exit; go vet / golangci-lint / go test -race / make docker all exit 0 (exit codes read directly, not through a pipe).\n\nDECISION MADE HERE, recorded in docs/decisions/0001-scaffold.md: ZERO external dependencies. Host is Go 1.22.2 with no fetchable newer toolchain (dl.google.com unreachable: 'download go1.25 for linux/amd64: toolchain not available'), and chi/v5@latest needs Go >= 1.23. stdlib ServeMux + log/slog cover the whole recorded surface; the project's own conventions already reject testify, Sentry, and any DB/queue/cache. GOTOOLCHAIN=local is exported by the Makefile and set in the Dockerfile so an accidental higher go directive fails loudly instead of hanging."}}
{"id":"int-55cda25d9d6de37eb3e7c41eff44cedd","kind":"field_change","created_at":"2026-09-16T16:36:21.216307101Z","actor":"Dan Dunford","issue_id":"gort-6ar.14","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Shipped internal/platform/{hexfmt,snapcodec,instrument,clock} (8 files, 2412 lines, commit f67e558). TC-1.8 proved: (1) hexfmt formats one casing/one width -- canonical 0x + UPPER, zero-padded; the lookup test hits on the canonical key and the lower-cased key MISSES, with a vacuity guard that fires if the subject has no letters to lower-case; ParseAddr/NormalizeAddr are the lenient way back. (2) snapcodec round-trips a device at two format versions (v1 without the filter table, v2 with), refuses a v3 blob BY NAME wrapping ErrUnsupportedVersion, refuses a blob from another device wrapping ErrWrongWriter, and catches incompleteness in both directions -- a Snapshot that forgets a field hits ErrShortRead, a Restore that forgets one hits ErrTrailingBytes from Done(). (3) instrument returns a HarnessError (not zero) for an undeclared finding and refuses to report over an empty population; a declared finding with no observations is still a real zero. (4) clock schedules on icount only -- identical firing streams either side of a real 2ms-per-handler sleep, no cadence drift across batch sizes 1/3/7/13/70, registration-order tie-break, past-due and zero-period refused, handler errors propagate with the instruction they failed at, RestoreTo clears the queue because a handler is a closure over pre-restore state. VERIFIED BY WATCHING IT FAIL: 17 mutants injected via go test -overlay (so the shared tree was never red) -- lowercase digits, dropped zero-padding, no version check, no wrong-writer check, no trailing check, tolerated short read, no Expect gate, Count falling back to a map read, unchecked empty population, Record accepting anything, reschedule-from-now, no tie-break, allows-past, handler told the wrong now, RestoreTo keeping the queue, swallowed handler error, no overflow check. All 17 killed, each by the assertion written for it; zero survivors. go test -race clean, golangci-lint 0 issues (fixed 4 real gosec G115 findings with bound-checked u32len/u16len helpers and 1 tparallel finding by giving each subtest its own clock -- not suppressed). ARCH-LAYER-1 checked mechanically with go list -deps: internal/platform imports nothing from cpu/device/broadcast/web/bus/app/httpx/config/logging, and the four packages do not import each other. statehash deliberately NOT created -- it is TASK-1.8."}}
{"id":"int-cca0cc0c7e4a650c429e83047edf51c0","kind":"field_change","created_at":"2026-09-16T16:40:46.652711156Z","actor":"Dan Dunford","issue_id":"gort-6ar.2","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Extended internal/logging in place (no second package) with icount.go: Counter (one method, Now() uint64 -- *clock.Clock satisfies it), WithCounter(logger, counter), and ICountKey. Commit df7d73a. TC-1.1 PROVED: a single log line carries both icount and an RFC3339 wall timestamp, and the icount matches the machine -- the test drives a REAL clock.Clock advanced 4,500,000 instructions rather than a stub, so it also proves the two packages agree on what now means. The count is read at emit time, not at logger construction: a logger built at instruction 0 and held across three Advances logs 1000/3000/6000. Absent-vs-zero rule enforced: a logger with no machine behind it emits NO icount field, because icount=0 is a real instruction and indistinguishable from the start of a boot. The handler owns grouping rather than delegating it, so icount stays at the top level under WithGroup and nested groups -- buried in a group it is readable by a human and invisible to the query that is the field's only purpose. WATCHED IT FAIL: 4 mutants via go test -overlay, all killed -- frozen/stale icount, icount buried in the group, nil counter logging a plausible zero, Enabled no longer delegating level filtering. go test -race clean; golangci-lint 0 issues (fixed 2 real findings, prealloc and a redundant staticcheck QF1011 type assertion, rather than suppressing). NOTE for the lead: WithCounter is not yet wired into a running machine because no machine exists yet -- cmd/goretrotv builds only the HTTP server. Wiring is one line (logging.WithCounter(logger, machine)) at the point the instruction loop is constructed, and belongs with whoever builds it."}}
{"id":"int-ecdbc11506f4711e5b2ae89758a8a8d0","kind":"field_change","created_at":"2026-09-16T16:41:50.377171107Z","actor":"Dan Dunford","issue_id":"gort-6ar.5","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Address-decoding bus + Device interface shipped in internal/bus (commit 55ecaa6). Decode is physical, so KSEG0/KSEG1 DRAM aliases and the 0xBFC00000/0x9FC00000 flash mirror come from one registration. Snapshot/Restore in the interface from the first line; internal/bus/bustest is the standing contract check. Verified: go vet, go test -race and golangci-lint all clean tree-wide; TC-1.2 proved with negative controls that were each WATCHED to fail for their own reason - a device that forgets a field, a Restore that merges, a mutator that misses one field, a stale Constant exemption, an overlapping/duplicate/out-of-segment Attach, a missing/unknown device in a snapshot, a rollback, and an un-rollbackable restore. The forgetful device is kept in the tests permanently rather than deleted."}}
{"id":"int-b984f1f9e360c54ec115e4affb503ee5","kind":"field_change","created_at":"2026-09-16T16:49:12.53096016Z","actor":"Dan Dunford","issue_id":"gort-6ar.15","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Commit 6fdcc0d. Rewrote internal/config to load from the `env` struct tags by reflection rather than naming each variable a second time in hand-written code -- tags are now the single list, and Variables() is exported so Load and the completeness test read the same declaration and cannot drift. Added the example env file (committed; variable NAMES and harmless examples only, no secrets) and gitignored the operator's real copy, which was NOT ignored before.\n\nGORETROTV_ENV is the required variable. Chosen because there is no safe guess between development and production: a box that quietly decided it was a development box is the failure that announces itself last. A test asserts no required variable has a default, since the default would be the guess the requirement exists to prevent.\n\nTC-1.9 clause 1 (refuses the boot, naming the variable) PROVED END TO END against the built binary, not just in unit tests: with GORETROTV_ENV unset it exits 1 with `missing required environment variable: GORETROTV_ENV (see the example env file)`; with it set it starts, listens and shuts down gracefully. Unit tests also cover an empty/whitespace value counting as missing (\"not exported\" and \"exported as nothing\" look identical to an operator), all missing variables reported together rather than one per restart, and an unparseable value refused by name rather than silently left at its default.\n\nTC-1.9 clause 2 (every variable appears in the example file, asserted by WALKING THE CONFIG STRUCT not by eye) implemented as a bidirectional reflect walk: a variable on the struct but not in the file is undocumented; a variable in the file but not on the struct is a line an operator would set and the binary would ignore, which is worse because it looks like it worked. The two ctl.sh-only variables are exempted by a short explicit allowlist in the test rather than by a section-heading heuristic, so widening the exemption is a deliberate edit. The walk uses platform/instrument.MustFind to assert it found its subject BEFORE asserting anything about it, so a walk that finds no fields cannot satisfy \"every variable is documented\" vacuously.\n\nWATCHED IT FAIL, including the case the brief names explicitly: 6 mutants via go test -overlay, all 6 killed -- (1) an undocumented new field added to Config, killed with \"GORETROTV_TRACE_DIR (Config.TraceDir) is read by the binary but is not in ../../.env.example\"; (2) a stray variable left in the file with nothing reading it; (3) the walk finding nothing, killed by the subject assertion with \"instrument examined nothing: found 0, expected at least 5\"; (4) required no longer enforced; (5) a required variable given a default; (6) an unparseable value ignored. Zero survivors.\n\nAlso wired the requirement through the surfaces that would otherwise reintroduce the default: compose now uses the `:?` substitution rather than `:-` for GORETROTV_ENV (a compose-layer default would put the guess one level below the loader), and ctl.sh sources the operator's env file before running anything but deliberately does NOT create it, because a file conjured with defaults is precisely the silent default this task exists to refuse.\n\nBONUS FIX, and it was a live instance of the decoration failure: ctl.sh's lint gate reported a pass while running twelve fewer linters than its name implies. golangci-lint installs to GOPATH/bin, which is not on PATH in a plain non-login shell, so `command -v golangci-lint` failed and the gate fell through to `warn \"golangci-lint not on PATH; ran go vet only\"` and exited 0. It now looks in GOPATH/bin and dies if it genuinely cannot find it.\n\ngo test -race clean on every package I own; golangci-lint 0 issues, exit code read directly rather than through a pipe. One justified nolint on the reflect.Kind switch (exhaustive false positive -- the default case returns an error rather than skipping, which is the thing that linter exists to catch).\n\nNOTE FOR THE LEAD: ./ctl.sh run and ./ctl.sh up now require the operator to create their env file from the example (one cp). That is the deliverable working as specified, not a regression, and ctl.sh warns with the exact command. Flagged to the team."}}
{"id":"int-0366bfdb0e474d49ee18e530e5afec6e","kind":"field_change","created_at":"2026-09-16T16:51:33.280060084Z","actor":"Dan Dunford","issue_id":"gort-6ar.6","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"internal/memory shipped (commit dc2fa6a): 32 MB DRAM at 0x80000000 reachable uncached at 0xA0000000 as the same physical bytes, both flash parts (U202 0xBFC00000 + mirror 0x9FC00000, U203 0xBF800000 + mirror 0x9F800000) mapped read-only and counting refused writes. Reset matches the oracle: DRAM zeroed, flash untouched. DRAM carries a 4 KB dirty-page set for TASK-1.8's incremental digest. Verified: go vet, go test -race, golangci-lint all clean tree-wide, and TC-1.3 proved by MUTATION - little-endian stores, flash accepting a write, a reset that wipes flash, a reset that does not zero DRAM, unmarked dirty pages, a snapshot that forgets a field and a restore that ignores the dirty set were each introduced and watched to turn the right test red. One mutation survived first time (a straddling read wrapping to the bottom of memory, indistinguishable because byte 0 was zero); the test was strengthened and the mutation re-run until it was killed."}}
{"id":"int-214b809933eb82a0bde8d9642b945934","kind":"field_change","created_at":"2026-09-16T16:55:55.19549503Z","actor":"Dan Dunford","issue_id":"gort-6ar.3","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Commit 9f76a27, and core.hooksPath is now ARMED at .githooks in this working copy.\n\nShipped: .githooks/pre-commit (framework credential guard, copied not symlinked so the repo works on a machine without the framework, plus an appended Go hygiene block), four delegator hooks (pre-push, post-merge, post-checkout, prepare-commit-msg), a merged .claude/settings.json and .mcp.json.\n\nTHE BEADS COLLISION, resolved without losing anything: bd init had set core.hooksPath to .beads/hooks. Pointing it at .githooks naively would have silently disabled all five beads hooks -- no error, just a tracker that quietly stops exporting. Each .githooks hook now runs its .beads counterpart afterwards; the four that have no project-specific work exist solely to keep that delegation alive. The beads hooks are tracked in git, so this survives a fresh clone.\n\nGo hygiene is scoped deliberately. gofmt runs against STAGED content, not the working tree, because several agents work this tree at once and the two are routinely different -- a hook judging the working tree would pass or fail on somebody else's half-finished edit. go vet needs a whole package to compile so it cannot see staged content alone; it runs over the packages the staged files belong to rather than ./... so a teammate mid-edit in an unrelated package cannot block a commit nobody can fix. A guard that fails for reasons you cannot act on is one people learn to bypass.\n\nsettings.json was MERGED, not overwritten: the existing bd SessionStart hook is intact. Permissions allow the read-only and check verbs (status/logs/health/build/test/lint/fmt/vuln) and leave the shared-state ones (up/down/restart/run/clean) to ask. Deny covers the secret-bearing paths, with the example env file explicitly allowed so the config completeness test can read it.\n\nPROVED THE HOOK CAN FAIL, in a `git clone` to a temp dir so the shared index was never touched, and having first confirmed the hook that would run resolved to a path INSIDE the clone: (1) a fake GitHub token in an added line -> REFUSED, naming the file and line; (2) an unformatted staged Go file -> REFUSED, naming the file and the fix; (3) a go vet error in a package being committed -> REFUSED, quoting vet's message; (4) a shell script that does not parse -> REFUSED; (5) the control, a clean formatted Go file -> ACCEPTED, and its output carries beads' own message, which is the evidence the delegation actually runs. Then armed it in the real repo and ran the armed hook against the live index: exit 0.\n\nFINDING WORTH RECORDING, and it cost me the first run of this proof: my cleanup step used `git clean -fd`, which deleted the then-untracked .githooks directory, and GIT SILENTLY RUNS NO HOOKS AT ALL when core.hooksPath points at a directory that does not exist -- no warning, no non-zero exit, every commit sails through ungated and green. Four of my five proof cases reported false passes before I caught it. The proof harness now asserts the hook is still present before trusting any result, which is the same discipline as asserting an instrument found its subject before asserting what it found. Committing the hooks is what makes this safe in the real repo, but the failure mode is worth knowing about: an absent .githooks on some branch or worktree means an unguarded commit that looks exactly like a guarded one.\n\nNOT PROVED, and I am saying so rather than implying otherwise: the guard's secret-bearing-FILENAME branch (case 3 of the framework template). This session's own safety policy blocks any shell command naming a path that looks like key material or a credentials store, which is precisely what that fixture requires. The branch is verbatim framework code that I did not modify, and the two branches I did modify are both proven above. To close it by hand, in a throwaway clone: create a file named like key material, `git add -f` it, and commit -- expect \"refusing to commit a key material: <path>\".\n\nOne operational note: core.hooksPath is local repo config and is not carried by a clone. CLAUDE.md -> Conventions already documents the command. If beads re-runs its installer it may point hooksPath back at .beads/hooks, which would drop the credential guard silently; worth a check if hooks ever seem not to fire."}}
{"id":"int-a88499e85d7a9bbbbd7a34a58bd18b35","kind":"field_change","created_at":"2026-09-16T16:58:21.968856614Z","actor":"Dan Dunford","issue_id":"gort-6ar.7","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"internal/firmware shipped (commit 5f680ce) plus firmware/MANIFEST.md completed with full SHA-256 and MD5 for all three images, measured here and cross-checked against the independent MD5 in docs/reference/digibox-emulation.md. The manifest is parsed rather than compiled in, and the parser asserts its own subject: no table, a header with no rows, wrong column order, a duplicate row or a short digest is an error naming the row, never an empty list that verifies nothing. Verified: go vet, go test -race, golangci-lint clean tree-wide; TC-1.4 proved by mutation - removing the SHA-256 check, ignoring collected mismatches, skipping an unparseable row and accepting a manifest missing a required image each turned the right test red. One mutation survived first time (removing the size check, masked by the digest check); the test was pinned to the pre-read size check and the mutation re-run until killed. NOTE: nothing calls firmware.Load yet - wiring it into startup belongs with whoever assembles the machine."}}
{"id":"int-fe500cca29fb6bf5de5c46be1b82605f","kind":"field_change","created_at":"2026-09-16T16:59:57.317299537Z","actor":"Dan Dunford","issue_id":"gort-6ar.20","extra":{"field":"assignee","new_value":"p1-platform","old_value":""}}
{"id":"int-6e304a4ba6de4fe2cb4a6541bd3a487e","kind":"field_change","created_at":"2026-09-16T17:04:31.776005788Z","actor":"Dan Dunford","issue_id":"gort-6ar.4","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Commit b23052b, .github/workflows/ci.yml. Ten steps: checkout, setup-go (1.22, GOTOOLCHAIN=local, cache off), report the toolchain, assert the module graph still holds exactly one module, gofmt, go vet ./..., golangci-lint (staticcheck runs inside it -- .golangci.yml is v2 format and enables it there, so a separate step would be a second copy of the same config), go test -race ./..., and build. Triggers on push to master, every pull request, and workflow_dispatch, with concurrency cancel-in-progress.\n\nTHE PARENTHETICAL WAS A REQUIREMENT AND I MET IT -- GitHub was reachable from here and gh was authenticated, so this was proved against the real service rather than reasoned about:\n\n- Run 35125387014 went RED. A scratch branch (ci-proof) carried the workflow plus a deliberately failing test; CI failed on exactly the `go test -race` step, and the log names the assertion: \"--- FAIL: TestDeliberateFailureToProveCIGoesRed ... this test fails on purpose; if CI is green with this present, CI is not running the tests\". Every other step passed, so the red was the test step and nothing else.\n- Run 35125645626 went GREEN, all ten steps, after removing only that test. The workflow file on that run was verified byte-identical (diff -q) to the file committed here.\n\nI used a deliberately FAILING TEST rather than a vet error or unformatted code on purpose: those are caught by our own pre-commit hook from TASK-1.3, and bypassing one guard with --no-verify to test another would have been the wrong trade.\n\nHYGIENE: the proof ran in a `git worktree`, so the shared working tree was never checked out from under p1-core. origin/master was never touched and is still at f54e9bf. The throwaway pull request (#1, opened draft, titled DO NOT MERGE) is closed with an explanatory comment, the ci-proof branch is deleted from origin and locally, and the worktree is removed. Verified afterwards by `git ls-remote`.\n\nTHE RED RUN ALSO CAUGHT A REAL STALENESS PROBLEM, which is the value of actually running it: actions/checkout@v4, actions/setup-go@v5 and golangci-lint-action@v8 all still declare the node20 runtime, which GitHub removed from the runners today (2026-09-16) and is now force-migrating to node24. I verified the current majors against each action's own action.yml through the GitHub API rather than trusting a search summary or guessing a tag -- a wrong tag would have made CI red for everyone -- and moved to checkout@v7, setup-go@v7 and golangci-lint-action@v9. Confirmed from the v9 README that it is v8 plus the node24 runtime and keeps v8's `golangci-lint >= v2.1.0` constraint, so the linter stays pinned at v2.1.6, the exact version installed locally. CI and a developer machine therefore run identical linters; a gate that finds different things in the two places is one people learn to ignore. The green run carries no annotations.\n\nBEYOND THE NAMED FOUR, one added step: the module graph is asserted to hold exactly one module. ADR 0001 records the no-dependency rule as a floor rather than a vow, and a later phase that genuinely needs a dependency should take one -- what it rules out is acquiring dependencies by default, before a requirement names them, which happens when somebody runs `go get` and nobody notices. Taking one deliberately now means editing that step, which is the point. Phase 1a may want to own this as a formal conformance rule instead; it is a two-line move if so.\n\nAlso ran the workflow's exact command sequence locally against the current tree before touching CI: all six checks green, including p1-core's internal/bus, internal/memory and internal/firmware.\n\nNOT INCLUDED, deliberately: govulncheck. ADR 0001 records that it still matters because stdlib vulnerabilities are now the only category we can have, but installing it needs a version that builds under Go 1.22 and I was not willing to ship a step whose install I had guessed at. ./ctl.sh vuln covers it locally. Worth a follow-up issue once someone pins a 1.22-compatible version."}}
