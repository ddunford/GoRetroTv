# Generated by the /document skill's docs-site.py — rebuild rather than commit.
# 1.1 MB per build, and every regeneration would land whole in history.
#   python3 docs-site.py build --docs docs --title "GoRetroTV"
#   python3 docs-site.py check --docs docs     # exit 1 when it has fallen behind
site.html
